# TRACE 0.3.0 — Sefaria Importer + Mishna Avot 1 Demo

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add a Sefaria API importer to TRACE (`tracealign.io.sefaria`) and ship it as v0.3.0; then build the first public real-world demo in the separate `tracealign-demos` repo: Mishna Avot chapter 1 aligned across William Davidson, Vilna, and Kaufmann.

**Architecture:** Two phases.
- **Phase A** (TRACE main repo, Tasks 0–10): new `tracealign.io.sefaria` importer using stdlib `urllib.request` (no new dependencies); tests use offline fixture responses; release as 0.3.0.
- **Phase B** (separate `tracealign-demos` repo, Tasks 11–17): clone repo, scaffold a Jupyter notebook that uses TRACE 0.3.0 to load three Sefaria versions of Mishna Avot 1, run pairwise and multi-witness alignment, render the variant graph + aligned table, and dump the result to JSON. CI executes the notebook headlessly.

**Tech Stack:** Python 3.10+, stdlib `urllib.request` (no new TRACE dependency), `tracealign>=0.3.0` in the demo repo, Jupyter, nbclient for headless execution in CI.

**Reference documents:**
- TRACE v0.1 design: [`docs/superpowers/specs/2026-04-28-trace-v0.1-design.md`](../specs/2026-04-28-trace-v0.1-design.md)
- TRACE v0.2 design: [`docs/superpowers/specs/2026-05-21-trace-v0.2-multi-witness-design.md`](../specs/2026-05-21-trace-v0.2-multi-witness-design.md)
- ROADMAP: [`docs/ROADMAP.md`](../../ROADMAP.md)

**Branch model:** Phase A on `feature/v0.3-sefaria-importer` off `develop`, PR to `develop`, release/0.3.0 off `main`. Phase B in a separate repo at `git@github.com:bsesic/tracealign-demos.git` (already created); work directly on its `main`.

**Quality gates** (Phase A): `flake8 src/ tests/` clean, `pytest -q` green on Python 3.10/3.11/3.12 via CI. **(Phase B)**: `nbclient` executes the demo notebook in CI without error.

**Language:** All code, comments, commit messages, issues, and READMEs in English.

**Author:** Use Benjamin's existing git config (`bsesic` / `Benjamin-777@gmx.de`). No AI attribution anywhere.

---

## Task Index

### Phase A — Sefaria importer in TRACE 0.3.0

| # | Task |
|---|---|
| 0 | Bootstrap — feature branch off develop |
| 1 | Sefaria fixture responses for tests |
| 2 | `_fetch_json` URL fetcher with mock injection point |
| 3 | `load(ref, version, lang)` — single-segment Hebrew |
| 4 | `load_segments` — chapter view returns list per segment |
| 5 | Strip embedded HTML tags from Sefaria text |
| 6 | `load_versions(ref, versions)` — convenience for multi-witness |
| 7 | Module-import test pinning the public surface |
| 8 | docs/usage.md gains Sefaria section; README updated |
| 9 | ROADMAP marks Sefaria importer as released |
| 10 | Release 0.3.0 — version bump, PR, merge, tag, push, build, twine check |

### Phase B — Mishna Avot 1 demo in `tracealign-demos` repo

| # | Task |
|---|---|
| 11 | Clone `tracealign-demos`, bootstrap structure |
| 12 | Notebook scaffold |
| 13 | Notebook: load three Sefaria versions |
| 14 | Notebook: pairwise alignment Davidson vs Vilna for Mishna 1 |
| 15 | Notebook: multi-witness alignment across the chapter |
| 16 | Notebook: render guide tree + aligned table + variant loci; JSON round-trip |
| 17 | CI workflow: nbclient executes the notebook headlessly |

---

## Phase A — Sefaria importer

## Task 0: Bootstrap

- [ ] **Step 1: Start from a clean develop**

```bash
git checkout develop
git pull --ff-only origin develop
git checkout -b feature/v0.3-sefaria-importer
```

- [ ] **Step 2: Verify baseline**

`pytest -q` → 182 passed. `flake8 src/ tests/` → no output.

- [ ] **Step 3: Verify develop's version**

```bash
grep -E "^version|__version__" pyproject.toml src/tracealign/__init__.py | head -3
```
Expected: both `0.3.0.dev0`.

No commit — branch existing is sufficient.

---

## Task 1: Sefaria fixture responses

**Files:** `tests/io/fixtures/sefaria_avot_1_{davidson,vilna,kaufmann}.json`

- [ ] **Step 1: Create Davidson fixture**

`tests/io/fixtures/sefaria_avot_1_davidson.json`:

```json
{
  "ref": "Pirkei Avot 1",
  "heRef": "פרקי אבות א",
  "versions": [
    {
      "versionTitle": "William Davidson Edition - Hebrew",
      "language": "he",
      "text": [
        "מֹשֶׁה קִבֵּל תּוֹרָה מִסִּינַי, וּמְסָרָהּ לִיהוֹשֻׁעַ, וִיהוֹשֻׁעַ לִזְקֵנִים, וּזְקֵנִים לִנְבִיאִים, וּנְבִיאִים מְסָרוּהָ לְאַנְשֵׁי כְנֶסֶת הַגְּדוֹלָה. הֵם אָמְרוּ שְׁלשָׁה דְבָרִים, הֱווּ מְתוּנִים בַּדִּין, וְהַעֲמִידוּ תַלְמִידִים הַרְבֵּה, וַעֲשׂוּ סְיָג לַתּוֹרָה.",
        "שִׁמְעוֹן הַצַּדִּיק הָיָה מִשְּׁיָרֵי כְנֶסֶת הַגְּדוֹלָה. הוּא הָיָה אוֹמֵר, עַל שְׁלשָׁה דְבָרִים הָעוֹלָם עוֹמֵד, עַל הַתּוֹרָה וְעַל הָעֲבוֹדָה וְעַל גְּמִילוּת חֲסָדִים.",
        "אַנְטִיגְנוֹס אִישׁ סוֹכוֹ קִבֵּל מִשִּׁמְעוֹן הַצַּדִּיק. הוּא הָיָה אוֹמֵר, אַל תִּהְיוּ כַעֲבָדִים הַמְשַׁמְּשִׁין אֶת הָרַב עַל מְנָת לְקַבֵּל פְּרָס."
      ]
    }
  ]
}
```

- [ ] **Step 2: Create Vilna fixture**

`tests/io/fixtures/sefaria_avot_1_vilna.json`:

```json
{
  "ref": "Pirkei Avot 1",
  "heRef": "פרקי אבות א",
  "versions": [
    {
      "versionTitle": "Vilna Edition",
      "language": "he",
      "text": [
        "משה קבל תורה מסיני ומסרה ליהושע ויהושע לזקנים וזקנים לנביאים ונביאים מסרוה לאנשי כנסת הגדולה. הם אמרו שלשה דברים הוו מתונים בדין והעמידו תלמידים הרבה ועשו סייג לתורה.",
        "שמעון הצדיק היה משירי כנסת הגדולה. הוא היה אומר על שלשה דברים העולם עומד על התורה ועל העבודה ועל גמילות חסדים.",
        "אנטיגנוס איש סוכו קבל משמעון הצדיק. הוא היה אומר אל תהיו כעבדים המשמשין את הרב על מנת לקבל פרס."
      ]
    }
  ]
}
```

- [ ] **Step 3: Create Kaufmann fixture (plene spellings)**

`tests/io/fixtures/sefaria_avot_1_kaufmann.json`:

```json
{
  "ref": "Pirkei Avot 1",
  "heRef": "פרקי אבות א",
  "versions": [
    {
      "versionTitle": "Kaufmann Manuscript",
      "language": "he",
      "text": [
        "מושה קבל תורה מסיני ומסרה ליהושוע ויהושוע לזקנים וזקנים לנביאים ונביאים מסרוה לאנשי כנסת הגדולה. הם אמרו שלושה דברים הוו מתונים בדין והעמידו תלמידים הרבה ועשו סייג לתורה.",
        "שמעון הצדיק היה משירי כנסת הגדולה. הוא היה אומר על שלושה דברים העולם עומד על התורה ועל העבודה ועל גמילות חסדים.",
        "אנטיגנוס איש סוכו קבל משמעון הצדיק. הוא היה אומר אל תהיו כעבדים המשמשין את הרב על מנת לקבל פרס."
      ]
    }
  ]
}
```

- [ ] **Step 4: Verify**

```bash
python -m json.tool tests/io/fixtures/sefaria_avot_1_davidson.json > /dev/null
python -m json.tool tests/io/fixtures/sefaria_avot_1_vilna.json > /dev/null
python -m json.tool tests/io/fixtures/sefaria_avot_1_kaufmann.json > /dev/null
pytest -q
```
Expected: silent JSON validation, 182 passed.

- [ ] **Step 5: Commit**

```bash
git add tests/io/fixtures/sefaria_avot_1_davidson.json \
        tests/io/fixtures/sefaria_avot_1_vilna.json \
        tests/io/fixtures/sefaria_avot_1_kaufmann.json
git commit -m "test(io/sefaria): add Mishna Avot 1 fixture responses for three versions

Saved Sefaria v3 API response shapes for William Davidson (Hebrew with
niqqud), Vilna Edition (plain), and Kaufmann Manuscript (plene). Three
witnesses, three mishnayot, intentional Davidson/Vilna/Kaufmann
differences to exercise EXACT, NIQQUD_STRIPPED, and PLENE_DEFECTIVE tiers."
```

---

## Task 2: `_fetch_json` with mock injection point

**Files:** `src/tracealign/io/sefaria.py`, `tests/io/test_sefaria_fetch.py`

- [ ] **Step 1: Failing test**

`tests/io/test_sefaria_fetch.py`:

```python
"""Tests for tracealign.io.sefaria._fetch_json mocking surface."""

import json
from pathlib import Path
from unittest.mock import patch

from tracealign.io import sefaria


FIXTURE_DIR = Path(__file__).parent / "fixtures"


def test_fetch_json_returns_parsed_json():
    payload = {"ref": "Pirkei Avot 1", "versions": []}
    with patch.object(sefaria, "_http_get", return_value=json.dumps(payload).encode("utf-8")):
        result = sefaria._fetch_json("https://example.invalid/")
    assert result == payload


def test_fetch_json_decodes_utf8():
    payload = {"text": "שלום"}
    with patch.object(sefaria, "_http_get", return_value=json.dumps(payload).encode("utf-8")):
        result = sefaria._fetch_json("https://example.invalid/")
    assert result["text"] == "שלום"


def test_fetch_json_uses_fixture():
    body = (FIXTURE_DIR / "sefaria_avot_1_davidson.json").read_bytes()
    with patch.object(sefaria, "_http_get", return_value=body):
        result = sefaria._fetch_json("https://example.invalid/")
    assert result["ref"] == "Pirkei Avot 1"
    assert result["versions"][0]["versionTitle"].startswith("William Davidson")
```

- [ ] **Step 2: Run; expect ImportError/ModuleNotFoundError**

`pytest tests/io/test_sefaria_fetch.py -v`

- [ ] **Step 3: Implement**

`src/tracealign/io/sefaria.py`:

```python
"""Sefaria public API importer.

Loads texts from the Sefaria API (https://www.sefaria.org/api/v3/texts/...)
and returns lists of TRACE Tokens. Tests inject mock HTTP responses by
patching the module-level `_http_get` function.
"""

from __future__ import annotations

import json
from urllib.request import Request, urlopen


def _http_get(url: str) -> bytes:
    """Fetch a URL and return the raw response body.

    Tests patch this function to return canned bytes instead of hitting
    the network.
    """
    req = Request(url, headers={"User-Agent": "tracealign/0.3.0"})
    with urlopen(req, timeout=30) as resp:
        return resp.read()


def _fetch_json(url: str) -> dict:
    """Fetch a URL and parse the response body as JSON (UTF-8)."""
    body = _http_get(url)
    return json.loads(body.decode("utf-8"))
```

- [ ] **Step 4: Run; expect 3 passed**

- [ ] **Step 5: Full suite + flake8**

`pytest -q` → 185 passed. `flake8 src/ tests/` → no output.

- [ ] **Step 6: Commit**

```bash
git add src/tracealign/io/sefaria.py tests/io/test_sefaria_fetch.py
git commit -m "feat(io/sefaria): add _fetch_json with stdlib urllib + mock injection

Single GET against a Sefaria API URL, JSON-parsed and returned as dict.
Module-level _http_get is the patch target for tests; _fetch_json wraps
it with UTF-8 decoding and json.loads. No external HTTP dependency."
```

---

## Task 3: `load(ref, version, lang)` — single segment

**Files:** modify `src/tracealign/io/sefaria.py`, create `tests/io/test_sefaria_load.py`

- [ ] **Step 1: Failing test**

`tests/io/test_sefaria_load.py`:

```python
"""Tests for tracealign.io.sefaria.load."""

import json
from pathlib import Path
from unittest.mock import patch

from tracealign.io import sefaria
from tracealign.model import Token


FIXTURE_DIR = Path(__file__).parent / "fixtures"


def _load_fixture(name: str) -> dict:
    return json.loads((FIXTURE_DIR / name).read_text(encoding="utf-8"))


def test_load_single_segment_returns_tokens():
    payload = _load_fixture("sefaria_avot_1_davidson.json")
    with patch.object(sefaria, "_fetch_json", return_value=payload):
        tokens = sefaria.load(
            "Pirkei Avot 1:1",
            version="William Davidson Edition - Hebrew",
            seq_label="Davidson",
        )
    assert isinstance(tokens, list)
    assert tokens, "expected at least one token"
    assert all(isinstance(t, Token) for t in tokens)
    assert tokens[0].raw.startswith("מ")


def test_load_single_segment_passes_seq_label_through():
    payload = _load_fixture("sefaria_avot_1_davidson.json")
    with patch.object(sefaria, "_fetch_json", return_value=payload):
        tokens = sefaria.load(
            "Pirkei Avot 1:1",
            version="William Davidson Edition - Hebrew",
            seq_label="Davidson",
        )
    assert all(t.id.startswith("Davidson:") for t in tokens)
```

- [ ] **Step 2: Run; expect AttributeError**

- [ ] **Step 3: Implement**

Append to `src/tracealign/io/sefaria.py`:

```python
from urllib.parse import quote

import tracealign
from tracealign.lang.base import LanguagePack
from tracealign.model import Token


SEFARIA_API_BASE = "https://www.sefaria.org/api/v3/texts"


def _build_url(ref: str, version: str | None) -> str:
    """Build the v3 API URL for a reference, optionally pinning a version."""
    encoded_ref = quote(ref.replace(" ", "_"), safe="._")
    url = f"{SEFARIA_API_BASE}/{encoded_ref}"
    if version is not None:
        url += f"?version=he|{quote(version, safe='')}"
    return url


def _select_version(payload: dict, version: str | None) -> dict:
    """Pick the right version block from the payload."""
    versions = payload.get("versions", [])
    if not versions:
        raise ValueError(f"Sefaria response has no versions: ref={payload.get('ref')!r}")
    if version is None:
        return versions[0]
    for v in versions:
        if v.get("versionTitle") == version:
            return v
    available = [v.get("versionTitle") for v in versions]
    raise ValueError(
        f"version {version!r} not found in Sefaria response; available: {available}"
    )


def load(
    ref: str,
    version: str | None = None,
    lang: str | LanguagePack = "hbo",
    seq_label: str | None = None,
) -> list[Token]:
    """Load one Sefaria reference and return its tokens.

    For chapter-level references that resolve to multiple segments the
    function concatenates all segments with single spaces. See
    `load_segments` to get a list of token-lists per segment.
    """
    url = _build_url(ref, version)
    payload = _fetch_json(url)
    version_block = _select_version(payload, version)
    text_field = version_block.get("text")
    if isinstance(text_field, list):
        text = " ".join(s for s in text_field if isinstance(s, str))
    elif isinstance(text_field, str):
        text = text_field
    else:
        raise ValueError(f"Sefaria version {version!r} has no text field")

    label = seq_label or version or ref
    return tracealign.tokenize(text, lang=lang, seq_label=label)
```

- [ ] **Step 4: Run; expect 2 passed**

- [ ] **Step 5: Full suite + flake8**

`pytest -q` → 187 passed. `flake8 src/ tests/` → no output.

- [ ] **Step 6: Commit**

```bash
git add src/tracealign/io/sefaria.py tests/io/test_sefaria_load.py
git commit -m "feat(io/sefaria): add load() for a Sefaria reference + version

Fetches one reference from the Sefaria v3 API (mockable via _fetch_json),
selects the requested versionTitle, concatenates segment strings with
spaces, and runs the result through tracealign.tokenize with the
caller-supplied seq_label."
```

---

## Task 4: `load_segments` — list per mishna

**Files:** modify `src/tracealign/io/sefaria.py`, modify `tests/io/test_sefaria_load.py`

- [ ] **Step 1: Append failing tests**

Append to `tests/io/test_sefaria_load.py`:

```python
def test_load_segments_returns_one_list_per_mishna():
    payload = _load_fixture("sefaria_avot_1_davidson.json")
    with patch.object(sefaria, "_fetch_json", return_value=payload):
        segments = sefaria.load_segments(
            "Pirkei Avot 1",
            version="William Davidson Edition - Hebrew",
            seq_label="Davidson",
        )
    assert len(segments) == 3
    assert all(isinstance(seg, list) for seg in segments)
    assert all(seg for seg in segments)


def test_load_segments_assigns_distinct_seq_labels_per_segment():
    payload = _load_fixture("sefaria_avot_1_davidson.json")
    with patch.object(sefaria, "_fetch_json", return_value=payload):
        segments = sefaria.load_segments(
            "Pirkei Avot 1",
            version="William Davidson Edition - Hebrew",
            seq_label="Davidson",
        )
    for i, seg in enumerate(segments):
        assert all(t.id.startswith(f"Davidson:{i + 1}:") for t in seg)
```

- [ ] **Step 2: Run; expect AttributeError**

- [ ] **Step 3: Implement**

Append to `src/tracealign/io/sefaria.py`:

```python
def load_segments(
    ref: str,
    version: str | None = None,
    lang: str | LanguagePack = "hbo",
    seq_label: str | None = None,
) -> list[list[Token]]:
    """Load one Sefaria reference and return one token list per segment.

    Each segment gets a derived seq_label of the form "{base}:{n}" where
    base defaults to the version title and n is 1-based.
    """
    url = _build_url(ref, version)
    payload = _fetch_json(url)
    version_block = _select_version(payload, version)
    text_field = version_block.get("text")
    if not isinstance(text_field, list):
        raise ValueError(
            f"Sefaria version {version!r} returned a single segment; "
            f"use load() instead of load_segments()"
        )

    base_label = seq_label or version or ref
    out: list[list[Token]] = []
    for i, segment_text in enumerate(text_field, start=1):
        if not isinstance(segment_text, str) or not segment_text.strip():
            out.append([])
            continue
        seg_label = f"{base_label}:{i}"
        out.append(tracealign.tokenize(segment_text, lang=lang, seq_label=seg_label))
    return out
```

- [ ] **Step 4: Run; expect 4 passed**

- [ ] **Step 5: Full suite + flake8**

`pytest -q` → 189 passed.

- [ ] **Step 6: Commit**

```bash
git add src/tracealign/io/sefaria.py tests/io/test_sefaria_load.py
git commit -m "feat(io/sefaria): add load_segments for chapter-level references

Returns one token list per segment. Each segment gets a derived
seq_label of the form {base}:{n} so token IDs stay unique across
segments and across witnesses."
```

---

## Task 5: Strip embedded HTML before tokenising

**Files:** modify `src/tracealign/io/sefaria.py`, modify `tests/io/test_sefaria_load.py`

- [ ] **Step 1: Failing test**

Append to `tests/io/test_sefaria_load.py`:

```python
def test_load_strips_html_tags():
    payload = {
        "ref": "Pirkei Avot 1:1",
        "versions": [
            {
                "versionTitle": "Test",
                "language": "he",
                "text": "<b>שלום</b> <i>עולם</i><sup>1</sup>",
            }
        ],
    }
    with patch.object(sefaria, "_fetch_json", return_value=payload):
        tokens = sefaria.load("Pirkei Avot 1:1", version="Test", seq_label="T")
    texts = [t.text for t in tokens]
    assert "שלום" in texts
    assert "עולם" in texts
    assert "1" not in texts
    assert all("<" not in t.raw and ">" not in t.raw for t in tokens)
```

- [ ] **Step 2: Run; expect AssertionError**

- [ ] **Step 3: Implement `_strip_html` and wire into load/load_segments**

Append to `src/tracealign/io/sefaria.py`:

```python
import re


_HTML_TAG_RE = re.compile(r"<[^>]+>")
_FOOTNOTE_DIGIT_RE = re.compile(r"<sup[^>]*>.*?</sup>", flags=re.DOTALL)


def _strip_html(text: str) -> str:
    """Remove embedded HTML markup before tokenising.

    Footnote markers inside <sup>...</sup> are dropped entirely; other
    tags are removed while inner text is preserved.
    """
    without_footnotes = _FOOTNOTE_DIGIT_RE.sub("", text)
    return _HTML_TAG_RE.sub("", without_footnotes)
```

In `load()`, replace the final `return tracealign.tokenize(...)` line with:

```python
    text = _strip_html(text)
    return tracealign.tokenize(text, lang=lang, seq_label=label)
```

In `load_segments()`, replace the `out.append(tracealign.tokenize(segment_text, ...))` block with:

```python
        cleaned = _strip_html(segment_text)
        if not cleaned.strip():
            out.append([])
            continue
        out.append(tracealign.tokenize(cleaned, lang=lang, seq_label=seg_label))
```

- [ ] **Step 4: Run; expect 1 passed**

- [ ] **Step 5: Full suite + flake8**

`pytest -q` → 190 passed.

- [ ] **Step 6: Commit**

```bash
git add src/tracealign/io/sefaria.py tests/io/test_sefaria_load.py
git commit -m "feat(io/sefaria): strip embedded HTML before tokenising

Sefaria text fields can contain <b>/<i>/<sup>. _strip_html removes all
tags; <sup>...</sup> footnote markers are dropped entirely so footnote
digits never pollute the token stream."
```

---

## Task 6: `load_versions(ref, versions)` — multi-witness convenience

**Files:** modify `src/tracealign/io/sefaria.py`, modify `tests/io/test_sefaria_load.py`

- [ ] **Step 1: Failing test**

Append to `tests/io/test_sefaria_load.py`:

```python
def test_load_versions_returns_dict_of_segments_per_version():
    davidson = _load_fixture("sefaria_avot_1_davidson.json")
    vilna = _load_fixture("sefaria_avot_1_vilna.json")
    kaufmann = _load_fixture("sefaria_avot_1_kaufmann.json")

    def fake_fetch(url):
        if "William%20Davidson" in url:
            return davidson
        if "Vilna" in url:
            return vilna
        if "Kaufmann" in url:
            return kaufmann
        raise AssertionError(f"unexpected URL: {url}")

    with patch.object(sefaria, "_fetch_json", side_effect=fake_fetch):
        out = sefaria.load_versions(
            "Pirkei Avot 1",
            versions=[
                "William Davidson Edition - Hebrew",
                "Vilna Edition",
                "Kaufmann Manuscript",
            ],
        )

    assert set(out.keys()) == {
        "William Davidson Edition - Hebrew",
        "Vilna Edition",
        "Kaufmann Manuscript",
    }
    for segments in out.values():
        assert len(segments) == 3
```

- [ ] **Step 2: Run; expect AttributeError**

- [ ] **Step 3: Implement**

Append to `src/tracealign/io/sefaria.py`:

```python
def load_versions(
    ref: str,
    versions: list[str],
    lang: str | LanguagePack = "hbo",
) -> dict[str, list[list[Token]]]:
    """Load multiple versions of one reference. One call per version.

    Returns dict[version_title, list[list[Token]]] — for each requested
    version, a list of token lists, one per segment.
    """
    return {
        v: load_segments(ref, version=v, lang=lang, seq_label=v)
        for v in versions
    }
```

- [ ] **Step 4: Run; expect 1 passed**

- [ ] **Step 5: Full suite + flake8**

`pytest -q` → 191 passed.

- [ ] **Step 6: Commit**

```bash
git add src/tracealign/io/sefaria.py tests/io/test_sefaria_load.py
git commit -m "feat(io/sefaria): add load_versions for multi-witness convenience

dict[version_title, list[list[Token]]] for one reference across multiple
versions. Each value is a list of token lists, one per segment."
```

---

## Task 7: Module-import test pinning the public surface

**Files:** create `tests/io/test_sefaria_reexport.py`

- [ ] **Step 1: Write the test**

`tests/io/test_sefaria_reexport.py`:

```python
"""Pin the public surface of tracealign.io.sefaria."""


def test_sefaria_module_importable():
    from tracealign.io import sefaria

    assert hasattr(sefaria, "load")
    assert hasattr(sefaria, "load_segments")
    assert hasattr(sefaria, "load_versions")
```

- [ ] **Step 2: Run; expect pass (no production code changes)**

`pytest tests/io/test_sefaria_reexport.py -v` → 1 passed.

- [ ] **Step 3: Full suite + flake8**

`pytest -q` → 192 passed.

- [ ] **Step 4: Commit**

```bash
git add tests/io/test_sefaria_reexport.py
git commit -m "test(io/sefaria): pin tracealign.io.sefaria module surface

Single hasattr test pinning load/load_segments/load_versions. No
top-level alias — the importer follows the existing
tracealign.io.{result,escriptorium,tei} import pattern."
```

---

## Task 8: Documentation — usage.md + README

**Files:** modify `docs/usage.md`, modify `README.md`

- [ ] **Step 1: Append Sefaria subsection to usage.md**

Before the "Custom lexica" section, insert:

````markdown
### Sefaria

```python
from tracealign.io import sefaria

segments = sefaria.load_segments(
    "Pirkei Avot 1",
    version="William Davidson Edition - Hebrew",
    seq_label="Davidson",
)
print(f"loaded {len(segments)} mishnayot")
```

For multi-witness work, fetch several versions at once:

```python
witnesses = sefaria.load_versions(
    "Pirkei Avot 1",
    versions=[
        "William Davidson Edition - Hebrew",
        "Vilna Edition",
        "Kaufmann Manuscript",
    ],
)
```

Sefaria's API is hit live; HTML markup is stripped automatically before tokenisation.
````

- [ ] **Step 2: Update README's I/O bullet**

Find:

```
- **I/O** for plain text, JSON (round-trip for both pairwise and multi-witness results), eScriptorium exports (with bbox + line metadata), and TEI XML (`<tei:w>` mode + flow-text fallback).
```

Replace with:

```
- **I/O** for plain text, JSON (round-trip for both pairwise and multi-witness results), eScriptorium exports, TEI XML, and Sefaria's public API (`tracealign.io.sefaria.load_versions`).
```

- [ ] **Step 3: Verify Sphinx**

`sphinx-build -W -b html docs docs/_build/html` → build succeeded.

- [ ] **Step 4: Full suite + flake8**

`pytest -q` → 192 passed.

- [ ] **Step 5: Commit**

```bash
git add docs/usage.md README.md
git commit -m "docs: document the Sefaria importer

docs/usage.md gains a Sefaria subsection showing load_segments and
load_versions; README adds a one-line mention so the importer surfaces
in Highlights."
```

---

## Task 9: ROADMAP marks Sefaria importer ✅

**Files:** modify `docs/ROADMAP.md`

- [ ] **Step 1: Update the corpus-importers table**

Find `| Sefaria API | planned |` and replace with `| Sefaria API | ✅ released 0.3.0 |`.

- [ ] **Step 2: Bump Last-updated**

Set `**Last updated:**` to today's date. Leave the current-release line at 0.2.0 until the release branch ships.

- [ ] **Step 3: Commit**

```bash
git add docs/ROADMAP.md
git commit -m "docs(roadmap): mark Sefaria importer as released in 0.3.0"
```

---

## Task 10: Release 0.3.0

- [ ] **Step 1: Final pre-release verification**

`pytest -q` → 192 passed. `flake8 src/ tests/` → no output. `sphinx-build -W -b html docs docs/_build/html` → build succeeded.

- [ ] **Step 2: Push + open PR**

```bash
git push --set-upstream origin feature/v0.3-sefaria-importer
gh pr create --base develop --head feature/v0.3-sefaria-importer \
  --title "v0.3: Sefaria importer" \
  --body "Adds tracealign.io.sefaria (load / load_segments / load_versions) with offline-fixture tests for Davidson, Vilna, and Kaufmann editions of Mishna Avot 1. No new runtime dependencies. Foundation for the Mishna Avot demo in tracealign-demos. Suite at 192 tests."
```

- [ ] **Step 3: After CI green, merge**

`gh pr merge <pr_number> --merge`

- [ ] **Step 4: Cut release/0.3.0 off main**

```bash
git checkout main
git pull --ff-only origin main
git checkout -b release/0.3.0
git merge --no-ff develop -m "Merge develop for v0.3.0 release"
```

Resolve merge conflicts:
- `pyproject.toml`: `version = "0.3.0"`
- `src/tracealign/__init__.py`: `__version__ = "0.3.0"`
- `CITATION.cff`: `version: "0.3.0"`, `date-released:` today; version DOI placeholder: `value: "10.5281/zenodo.20440062"`, `description: "Version DOI for TRACE 0.2.0 (will be replaced with the 0.3.0 archive DOI once Zenodo archives the GitHub Release)."`
- `docs/ROADMAP.md`: header current-release line → 0.3.0

```bash
git add pyproject.toml src/tracealign/__init__.py CITATION.cff docs/ROADMAP.md
git commit  # if there were merge conflicts to commit
pytest -q                       # 192 passed
flake8 src/ tests/              # no output
```

- [ ] **Step 5: Merge to main, tag, push**

```bash
git checkout main
git merge --no-ff release/0.3.0 -m "Release 0.3.0

Sefaria importer (tracealign.io.sefaria) enables multi-witness alignment
of rabbinic corpora published via Sefaria's public API. Foundation for
the Mishna Avot demo in tracealign-demos."
git tag -a 0.3.0 -m "TRACE 0.3.0 — Sefaria importer."
git push origin main 0.3.0
git branch -d release/0.3.0
```

- [ ] **Step 6: Build + twine check**

```bash
rm -rf dist/ build/ src/*.egg-info
venv/bin/python -m build
venv/bin/twine check dist/*
```
Expected: both artefacts PASS.

- [ ] **Step 7: User PyPI upload**

```bash
TWINE_USERNAME=__token__ venv/bin/twine upload dist/tracealign-0.3.0*
```

- [ ] **Step 8: User publishes GitHub Release for 0.3.0**

Triggers Zenodo archive.

- [ ] **Step 9: Controller updates CITATION.cff with real DOI**

```bash
curl -s "https://zenodo.org/api/records?q=conceptrecid:20315408&sort=mostrecent&size=5" \
  | python -c "import sys, json; d=json.load(sys.stdin); [print(h['metadata']['version'], h['doi']) for h in d['hits']['hits']]"
```

Edit `CITATION.cff` on `develop`: replace placeholder DOI with the real 0.3.0 value. Cherry-pick the CITATION-only diff onto `main`.

- [ ] **Step 10: Bump develop to 0.4.0.dev0**

```bash
git checkout develop
git pull --ff-only origin develop
# Edit pyproject.toml + src/tracealign/__init__.py version → 0.4.0.dev0
git add pyproject.toml src/tracealign/__init__.py
git commit -m "chore(release): bump develop to 0.4.0.dev0"
git push
```

Phase A complete. **TRACE 0.3.0 is live on PyPI.**

---

## Phase B — Mishna Avot 1 demo

Work in a **separate repo**: `git@github.com:bsesic/tracealign-demos.git`. Clone into `/mnt/data/coding/tracealign-demos`.

## Task 11: Bootstrap the demo repo

**Working dir:** `/mnt/data/coding/tracealign-demos`

**Files:** `README.md`, `LICENSE`, `.gitignore`, `requirements.txt`

- [ ] **Step 1: Clone**

```bash
cd /mnt/data/coding
git clone git@github.com:bsesic/tracealign-demos.git
cd tracealign-demos
```

- [ ] **Step 2: README.md**

```markdown
# tracealign-demos

Reproducible, runnable demos of [TRACE](https://github.com/bsesic/trace) (`tracealign` on PyPI) on real corpora.

Each demo is a self-contained Jupyter notebook executed in CI so it cannot rot silently.

## Demos

| Demo | Corpus | Witnesses | Highlights |
|---|---|---|---|
| [`mishna-avot-1/`](mishna-avot-1/) | Sefaria — Mishna Avot 1 | William Davidson, Vilna, Kaufmann | Hebrew tokenisation, pairwise + multi-witness alignment, guide tree + variant graph + aligned table |

## Running locally

```bash
pip install -r requirements.txt
jupyter lab
```

## License

[MIT](LICENSE) © 2026 Benjamin Schnabel.
```

- [ ] **Step 3: LICENSE**

MIT, copyright 2026 Benjamin Schnabel (copy from main TRACE repo).

- [ ] **Step 4: .gitignore**

```
__pycache__/
*.py[cod]
.ipynb_checkpoints/
.venv/
venv/
*.egg-info/
.pytest_cache/
dist/
build/
.DS_Store
.idea/
.vscode/
```

- [ ] **Step 5: requirements.txt**

```
tracealign>=0.3.0
jupyterlab>=4.0
nbclient>=0.10
nbformat>=5.10
ipykernel>=6.29
```

- [ ] **Step 6: Initial commit + push**

```bash
git add README.md LICENSE .gitignore requirements.txt
git commit -m "Initial commit: README, license, requirements, gitignore"
git push origin main
```

---

## Task 12: Notebook scaffold

**Files:** `mishna-avot-1/README.md`, `mishna-avot-1/multi-witness.ipynb`, `mishna-avot-1/data/.gitkeep`

- [ ] **Step 1: Per-demo README**

`mishna-avot-1/README.md`:

```markdown
# Mishna Avot Chapter 1 — Multi-Witness Demo

Aligns three Sefaria editions of [Pirkei Avot chapter 1](https://www.sefaria.org/Pirkei_Avot.1) — William Davidson, Vilna, Kaufmann — using `tracealign.align_multi`.

## What you'll see

- Three Hebrew versions of three mishnayot loaded via `tracealign.io.sefaria.load_versions`.
- Pairwise alignment of Davidson vs Vilna for the first mishna.
- Multi-witness alignment of all three versions across the chapter.
- JSON dump/load of the result.

## Running

```bash
pip install -r ../requirements.txt
jupyter lab multi-witness.ipynb
```
```

- [ ] **Step 2: Notebook scaffold**

`mishna-avot-1/multi-witness.ipynb`:

```json
{
  "cells": [
    {
      "cell_type": "markdown",
      "metadata": {},
      "source": [
        "# Mishna Avot 1 — Multi-Witness Alignment Demo\n",
        "\n",
        "Aligns three Sefaria editions of Pirkei Avot chapter 1 using `tracealign.align_multi`."
      ]
    },
    {
      "cell_type": "code",
      "execution_count": null,
      "metadata": {},
      "outputs": [],
      "source": [
        "import tracealign\n",
        "print('tracealign version:', tracealign.__version__)\n",
        "print('available languages:', tracealign.list_languages())"
      ]
    }
  ],
  "metadata": {
    "kernelspec": {"display_name": "Python 3 (ipykernel)", "language": "python", "name": "python3"},
    "language_info": {"name": "python", "version": "3.12"}
  },
  "nbformat": 4,
  "nbformat_minor": 5
}
```

- [ ] **Step 3: data/.gitkeep**

```bash
mkdir -p mishna-avot-1/data
touch mishna-avot-1/data/.gitkeep
```

- [ ] **Step 4: Verify notebook is valid JSON**

```bash
python -c "import nbformat; nbformat.read('mishna-avot-1/multi-witness.ipynb', as_version=4); print('ok')"
```

- [ ] **Step 5: Commit**

```bash
git add mishna-avot-1/
git commit -m "feat: scaffold mishna-avot-1 multi-witness notebook"
```

---

## Task 13: Notebook — load three Sefaria versions

**Files:** modify `mishna-avot-1/multi-witness.ipynb`

- [ ] **Step 1: Append two cells**

Append after the bootstrap cell:

```json
{
  "cell_type": "markdown",
  "metadata": {},
  "source": ["## 1. Load three versions of Mishna Avot 1 from Sefaria"]
}
```

```json
{
  "cell_type": "code",
  "execution_count": null,
  "metadata": {},
  "outputs": [],
  "source": [
    "from tracealign.io import sefaria\n",
    "\n",
    "VERSIONS = [\n",
    "    \"William Davidson Edition - Hebrew\",\n",
    "    \"Vilna Edition\",\n",
    "    \"Kaufmann Manuscript\",\n",
    "]\n",
    "\n",
    "witnesses = sefaria.load_versions(\"Pirkei Avot 1\", versions=VERSIONS)\n",
    "for name, segments in witnesses.items():\n",
    "    n_tokens = sum(len(seg) for seg in segments)\n",
    "    print(f\"{name}: {len(segments)} mishnayot, {n_tokens} tokens\")"
  ]
}
```

- [ ] **Step 2: Execute headlessly**

```bash
cd mishna-avot-1
python -m nbclient.cli multi-witness.ipynb --kernel python3
cd ..
```

If "Kaufmann Manuscript" is not the exact version title Sefaria uses, list available titles:

```bash
python -c "
from urllib.request import urlopen, Request
import json
req = Request('https://www.sefaria.org/api/v3/texts/Pirkei_Avot.1', headers={'User-Agent': 'tracealign/0.3.0'})
data = json.load(urlopen(req, timeout=30))
for v in data.get('versions', []):
    print(v['versionTitle'])
"
```

Pick the closest manuscript title (e.g. "Kaufmann Codex"). Update `VERSIONS` and re-execute.

- [ ] **Step 3: Commit**

```bash
git add mishna-avot-1/multi-witness.ipynb
git commit -m "feat(demo): load three Sefaria versions in the Avot 1 notebook"
```

---

## Task 14: Notebook — pairwise Davidson vs Vilna for Mishna 1

**Files:** modify `mishna-avot-1/multi-witness.ipynb`

- [ ] **Step 1: Append cells**

```json
{
  "cell_type": "markdown",
  "metadata": {},
  "source": [
    "## 2. Pairwise alignment: Davidson vs Vilna on Mishna 1\n",
    "\n",
    "Davidson's vocalised forms align to Vilna's plain forms via the NIQQUD_STRIPPED tier."
  ]
}
```

```json
{
  "cell_type": "code",
  "execution_count": null,
  "metadata": {},
  "outputs": [],
  "source": [
    "davidson_mishna_1 = witnesses[\"William Davidson Edition - Hebrew\"][0]\n",
    "vilna_mishna_1 = witnesses[\"Vilna Edition\"][0]\n",
    "\n",
    "result = tracealign.align(davidson_mishna_1, vilna_mishna_1, lang=\"hbo\")\n",
    "print(f\"total score: {result.total_score:.3f}\")\n",
    "print(f\"summary: {dict(result.summary)}\")\n",
    "\n",
    "for m in result.matches:\n",
    "    a = m.token_a.text if m.token_a else '—'\n",
    "    b = m.token_b.text if m.token_b else '—'\n",
    "    print(f\"  {a:>14}  ↔  {b:<14}  {m.reason.value:<18} {m.score:.2f}\")"
  ]
}
```

- [ ] **Step 2: Execute headlessly**

```bash
cd mishna-avot-1
python -m nbclient.cli multi-witness.ipynb --kernel python3
cd ..
```

- [ ] **Step 3: Commit**

```bash
git add mishna-avot-1/multi-witness.ipynb
git commit -m "feat(demo): pairwise alignment Davidson vs Vilna for Mishna 1"
```

---

## Task 15: Notebook — multi-witness across the chapter

**Files:** modify `mishna-avot-1/multi-witness.ipynb`

- [ ] **Step 1: Append cells**

```json
{
  "cell_type": "markdown",
  "metadata": {},
  "source": [
    "## 3. Multi-witness alignment across all three versions\n",
    "\n",
    "Each version's whole chapter becomes one witness; `tracealign.align_multi` builds one variant graph."
  ]
}
```

```json
{
  "cell_type": "code",
  "execution_count": null,
  "metadata": {},
  "outputs": [],
  "source": [
    "def _flatten(segments):\n",
    "    return [tok for seg in segments for tok in seg]\n",
    "\n",
    "flat = {name: _flatten(segs) for name, segs in witnesses.items()}\n",
    "for name, toks in flat.items():\n",
    "    print(f\"{name}: {len(toks)} tokens\")\n",
    "\n",
    "multi = tracealign.align_multi(flat, lang=\"hbo\")\n",
    "print(\"\\nwitness ids:\", multi.witness_ids)\n",
    "print(\"variant loci:\", len(list(multi.graph.variants())))"
  ]
}
```

- [ ] **Step 2: Execute headlessly + commit**

```bash
cd mishna-avot-1
python -m nbclient.cli multi-witness.ipynb --kernel python3
cd ..
git add mishna-avot-1/multi-witness.ipynb
git commit -m "feat(demo): multi-witness alignment across Davidson/Vilna/Kaufmann"
```

---

## Task 16: Notebook — guide tree, table, variants, JSON

**Files:** modify `mishna-avot-1/multi-witness.ipynb`

- [ ] **Step 1: Append inspection cells**

```json
{
  "cell_type": "markdown",
  "metadata": {},
  "source": [
    "## 4. Inspect the alignment\n",
    "\n",
    "- Guide tree (UPGMA)\n",
    "- Variant loci (graph nodes where witnesses disagree)\n",
    "- Aligned table (re-anchored at Vilna)"
  ]
}
```

```json
{
  "cell_type": "code",
  "execution_count": null,
  "metadata": {},
  "outputs": [],
  "source": [
    "print(\"## Guide tree (UPGMA)\")\n",
    "print(multi.guide_tree.format_text())\n",
    "\n",
    "print(\"\\n## Variant loci (first 12)\")\n",
    "for node in list(multi.graph.variants())[:12]:\n",
    "    readings = {wid: t.text for wid, t in node.tokens.items()}\n",
    "    print(f\"  {node.id}: {readings}\")\n",
    "\n",
    "print(\"\\n## Aligned table (re-anchored at Vilna, first 120 columns)\")\n",
    "print(multi.table.re_anchor(\"Vilna Edition\").format_text(max_columns=120))"
  ]
}
```

- [ ] **Step 2: Append persistence cells**

```json
{
  "cell_type": "markdown",
  "metadata": {},
  "source": ["## 5. Persist the result and read it back"]
}
```

```json
{
  "cell_type": "code",
  "execution_count": null,
  "metadata": {},
  "outputs": [],
  "source": [
    "from pathlib import Path\n",
    "from tracealign.io import multi_result as mr_io\n",
    "\n",
    "out_path = Path(\"data\") / \"avot_1_multi.json\"\n",
    "out_path.parent.mkdir(exist_ok=True)\n",
    "mr_io.dump(multi, out_path)\n",
    "print(f\"wrote {out_path.stat().st_size / 1024:.1f} KiB to {out_path}\")\n",
    "\n",
    "restored = mr_io.load(out_path)\n",
    "assert restored.witness_ids == multi.witness_ids\n",
    "assert len(list(restored.graph.variants())) == len(list(multi.graph.variants()))\n",
    "print(\"round-trip OK\")"
  ]
}
```

- [ ] **Step 3: Execute + commit**

```bash
cd mishna-avot-1
python -m nbclient.cli multi-witness.ipynb --kernel python3
cd ..
git add mishna-avot-1/multi-witness.ipynb
git commit -m "feat(demo): render guide tree, aligned table, variant loci, JSON round-trip"
```

---

## Task 17: CI executes the notebook on every push

**Files:** create `.github/workflows/notebooks.yml`

- [ ] **Step 1: Create workflow**

`.github/workflows/notebooks.yml`:

```yaml
name: notebooks

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  execute:
    name: execute notebooks (py${{ matrix.python-version }})
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.10", "3.11", "3.12"]
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
          cache: pip

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install -r requirements.txt

      - name: Register Jupyter kernel
        run: python -m ipykernel install --user --name python3 --display-name "Python 3 (ipykernel)"

      - name: Execute notebooks
        run: |
          for nb in $(find . -name '*.ipynb' -not -path '*/.ipynb_checkpoints/*'); do
            echo "Executing $nb"
            python -m nbclient.cli "$nb" --kernel python3 --timeout 600
          done
```

- [ ] **Step 2: Commit + push**

```bash
git add .github/workflows/notebooks.yml
git commit -m "ci: execute every notebook on push and PR"
git push origin main
```

- [ ] **Step 3: Verify**

```bash
gh run list --limit 1
```
Expected: `success`. If `tracealign==0.3.0` is not yet on PyPI when CI runs, the install step fails — confirms Phase A must complete first.

---

## Closing checklist

- [ ] Confirm `pip install tracealign==0.3.0` works from a fresh venv.
- [ ] Confirm the demo notebook runs end-to-end in CI on all three Python matrix entries.
- [ ] Optional: link to the demo repo from the main TRACE README under "Project status".
- [ ] Optional: TEI-list announcement with the demo as the anchor.
- [ ] Optional: start the JOSS paper using the demo as concrete methods example.
